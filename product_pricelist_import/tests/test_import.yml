-
    creo una categoria para el proveedor
-
    !record {model: product.category, id: category_1}:
        name: proov1
-
    creo un proveedor
-
    !record {model: res.partner, id: supplier_1}:
        name: proveedor uno
        categ_id: category_1
-
    creo el product pricelist
-
    !record {model: product.pricelist.load, id: product_pricelist_load_1}:
        name: Archivo de prueba
        date: !eval time.strftime('%Y-%m-%d')
        file_name: /mnt/extra-addons/jeo/product_pricelist_import/tests/test.xls
        supplier: supplier_1
        mode: 'append'
-
    Reading test.xls file
-
    !python {model: product.pricelist.load}: |
        from datetime import datetime
        import base64

        product_pricelist_load = self.pool['product.pricelist.load'].browse(cr,uid,ref('product_pricelist_load_1'))
        print product_pricelist_load.file_name

        import_file = self.pool['import.price.file']
        import_file.create(cr,uid,{'data':'1'})
        context['active_id'] = ref('product_pricelist_load_1')
        import_file.action_import(cr,uid,[ref('product_pricelist_load_1')],context)











